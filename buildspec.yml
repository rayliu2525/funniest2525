version: 0.2

phases:
  install:
    commands:
      - echo nothing to see here
  pre-build:
    commands:
      - echo nothing to see here
  build:
    commands:
      - echo nothing to see here
  post_build:
    commands:
      - echo nothing to see here